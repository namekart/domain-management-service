# =====================================================================
# DEV PROFILE — local development overrides
# =====================================================================

# ===== DATABASE =====
spring.datasource.url=jdbc:mysql://vps1.namekart.com:3306/axsiom_auctionhacker1?useSSL=true&requireSSL=false
spring.datasource.username=root
spring.datasource.password=NkMysqlRoot#2025!

# ===== LOGGING =====
logging.level.com.namekart.domainmanagement=DEBUG

# ===== TRACING / OBSERVABILITY (disabled for local dev) =====
management.tracing.sampling.probability=0.0
management.tracing.enabled=false
management.otlp.tracing.endpoint=
observability.remote.enabled=false
observability.tempo.endpoint=
observability.loki.url=

# ===== CORS =====
cors.allowed-origins=http://localhost:3000,http://localhost:8080,http://localhost:5173,http://localhost:4200,http://localhost:9000

# ===== SECURITY =====
domainmanagement.security.enforce=false
